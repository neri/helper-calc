# ライブコーディング用設計書: 助っ人貸出回数計算機

## 概要
- 「助っ人貸出回数計算機」を含む複数のサブアプリと、切り替え用のアプリスイッチャーを実装する。
- SPAとしてオフラインで動作し、必要なリソースの読み込み以外に外部と通信しない。

## 技術スタック
- **言語**: TypeScript
- **ビルドツール**: Vite
- **パッケージマネージャー**: pnpm
- **フレームワーク**: なし (Vanilla TypeScript + HTML/CSS)
- **テストフレームワーク**: Vitest
- **実行環境**: ブラウザ (SPA)

## 機能仕様

### メインアプリ (アプリスイッチャー)
- **アプリ切り替え**: ハンバーガーメニューから利用可能なサブアプリを選択して切り替え。
- **タイトル表示**: タイトルバーに現在実行中のサブアプリ名を表示。
- **URLフラグメント**:
  - サブアプリ切り替え時に `#helper` / `#tactical` / `#report` を設定。
  - 初期表示時にフラグメントを参照してサブアプリを切り替え。

### サブアプリ: 助っ人貸出回数計算機 (HelperCalcApp)
- **主要機能**:
  - ユーザーが「現在の報酬」と「経過時間」を入力 (「登録時間」(時間単位) または「設定日時」(YYYY-MM-DDTHH:MM形式、ロケール依存表示) のいずれかを選択)。
  - 入力に基づき、「貸し出し」回数を推定して表示。
  - 推定結果は入力が止まってから0.5秒後に更新 (デバウンス処理)。
  - 計算結果の貸出回数が0より小さい場合は、結果を表示しない。
  - 獲得報酬は20の倍数のみ有効 (それ以外は結果非表示)。
  - ローカルストレージ: 入力モード、登録時間、設定日時を保存 (獲得報酬は保存しない)。

- **計算ロジック**:
  - **報酬増加のルール**:
    - 時間経過: 1分ごとに20ポイント増加。
    - 貸し出し: 1回あたり50,000ポイント増加。
  - **時間の扱い**:
    - 登録時間入力時: 1時間単位 (例: 1時間、2時間)、実際の時間は1:00〜1:59の範囲 (60分〜119分) を考慮。
    - 設定日時入力時: 設定日時から現在までの経過時間を分単位で計算 (10分の誤差は許容)。
  - **推定式**:
    - 登録時間入力時: 推定貸出回数 C = floor( (獲得報酬 B - (登録時間 A × 60分) × 20) / 50,000 )
    - 設定日時入力時: 推定貸出回数 C = floor( (獲得報酬 B - (経過時間 A 分) × 20) / 50,000 )
    - Cが負の場合: エラー「貸出回数が負です」(結果非表示)
    - 時間報酬 D = 獲得報酬 B - (C × 50,000)
    - 貸出回数報酬 E = C × 50,000
    - 報酬の範囲を計算: 最大報酬 = ((A × 60) + 59) × 20 + C × 50,000 (登録時間時)、または (A + 10) × 20 + C × 50,000 (設定日時時)
    - 入力報酬が最大報酬以下なら確定、そうでなければエラー「獲得報酬の値が計算範囲外です」。

### サブアプリ: 対抗戦順位計算機 (TacticalRankCalcApp)
- **主要機能**:
  - ユーザーが「現在の順位」を入力。
  - 「次の順位」を計算するロジック:
    - 現在の順位が10以上の場合: `Math.floor((現在の順位 * 7) / 10)`
    - 現在の順位が10未満の場合: `現在の順位 - 3`
    - 計算結果が1以下の場合は1に補正。
  - 挑戦回数をselectタグで1~25から選択 (初期値5、localStorage保存)。
  - 計算結果を新しい現在の順位として、次の順位を指定回数繰り返し計算。
  - 計算中に1位が出た時点で以降の繰り返し処理を打ち切り。
  - 計算結果を「i回目：n位」のフォーマットで全て表示。
  - 順位ごとの報酬を表示 (石とコイン)。
  - 「現在の報酬見込」として現在の順位に対応する報酬を表示。
  - ランクが変動している場合は「最終報酬見込」として最終計算結果の順位に対応する報酬も表示。
  - 実際に繰り返した回数から最低石割り回数を計算: `floor((実際に繰り返した回数 + 4) / 5) - 1`
  - 石割り数の計算を表示。
  - ローカルストレージ: 入力した「現在の順位」と選択した「挑戦回数」を保存。

### サブアプリ: レポート計算機 (ReportCalcApp)
- **主要機能**:
  - 「レポート」4種類（初級・中級・上級・最上級）と「クレジット」を入力。
  - 入力はすべて整数（0以上）。
  - 入力停止後0.5秒のデバウンスで自動計算。
  - 「計算」ボタンで即時計算（デバウンスとは独立）。
  - ローカルストレージ: 4種類のレポート数とクレジットを保存・復元。

- **計算ロジック**:
  - EXP = 初級×50 + 中級×500 + 上級×2000 + 最上級×10000

### UI/UX 仕様
- **メインアプリ**:
  - **タイトルバー**: ハンバーガーメニューと現在実行中のアプリ名を表示。
  - **アプリメニュー**: ハンバーガーメニューから展開されるメニューで、サブアプリを選択。
  - **メニューの挙動**:
    - ハンバーガークリックでメニューを開閉（`app-menu.show`）。
    - メニュー選択でアプリ切替・アクティブ項目更新・メニュー自動クローズ。
      - メニューは固定幅で左からスライド表示。
      - メニュー表示中はメインコンテンツのみ暗くする（メニューとタイトルバーは明るさ維持）。
    - **Aboutダイアログ**:
      - メニューに「このアプリについて」を追加。
      - モーダルで概要文とGitHubリンク（表示名: 「GitHubで表示」）を表示。
      - このアプリは全ての計算をデバイス上で計算し、必要なリソースの読み込み以外に外部と通信しません。
      - レポート計算機のロジックの一部は[こちら](https://youtu.be/-DMS9jgtXPM)の動画を参考にさせていただきました。
- **サブアプリ: 助っ人貸出回数計算機**:
  - **入力フォーム**:
    - 入力モード選択: タブ風のボタンで「登録時間」または「設定日時」を選択 (二者択一)。
    - 登録時間: 数値入力フィールド (「登録時間」選択時のみ有効、正の整数、単位: 時間)。
    - 設定日時: 日時入力フィールド (「設定日時」選択時のみ有効、YYYY-MM-DDTHH:MM形式、ロケール依存表示)。
    - 獲得報酬: 数値入力フィールド (必須、正の整数、20の倍数のみ有効)。
  - **出力表示**:
    - 編成回数（推定）: 一番上に大きく表示 (3桁ごとにカンマ区切り、青色)。
    - 登録時間報酬: 獲得報酬から貸出回数報酬を引いた値 (3桁ごとにカンマ区切り)。
    - 編成回数報酬: 貸出回数 × 50,000 (3桁ごとにカンマ区切り)。
    - エラーメッセージ: 無効な入力時または計算範囲外時 (例: 負の数、ゼロ、または「獲得報酬の値が計算範囲外です」)。
  - **動作**:
    - 入力停止後0.5秒のデバウンスで自動計算（モード/時間/日時/報酬の変更に反応）。
    - 未来日時・無効な日時はUIでエラー表示。
    - 貸出回数が負の場合は結果非表示。
  - **ローカルストレージ**:
    - 保存する値: 入力モード、登録時間、設定日時。
    - 保存しない値: 獲得報酬。
- **サブアプリ: 対抗戦順位計算機**:
  - **入力フォーム**:
    - 「現在の順位」数値入力フィールド (最大5桁の整数、1~15001の範囲)。
    - 「挑戦回数」selectタグ (1~25、初期値5)。
    - 計算ボタン (クリックで即時計算)。
  - **出力表示**:
    - 計算結果を「i回目：n位」のフォーマットでリスト表示。
    - 「現在の報酬見込」: 現在の順位に対応する報酬 (石、コイン)。
    - 「最終報酬見込」: 最終計算結果の順位に対応する報酬 (ランク変動時のみ)。
    - 「最低石割り」: 最低石割り回数が1以上の場合のみ、総石数を含め「X回 Y個」形式で表示。
  - **動作**:
    - 入力変更（順位・挑戦回数）後0.5秒のデバウンスで自動計算。
    - 計算ボタンでも即時計算可能。
  - **ローカルストレージ**:
    - 保存する値: 入力した「現在の順位」、選択した「挑戦回数」。
- **サブアプリ: レポート計算機**:
  - **入力フォーム**:
    - 初級/中級/上級/最上級のレポート数（数値入力、0以上の整数）。
    - クレジット（M単位、数値入力、0以上の整数、5桁まで、サフィックス「M」を表示）。
    - 詳細設定（開閉可能、デフォルトは閉じる）:
      - 基準人数（1桁の整数、デフォルト5、未入力時はデフォルトに補正）。
      - 育成基準（2桁の整数、デフォルト50、サフィックス「M」を表示、未入力時はデフォルトに補正）。
    - 計算ボタン（クリックで即時計算）。
  - **出力表示**:
    - 最上部に独立した「特別依頼」グループを表示。
      - レポート1人分 = EXP / 1249200
      - クレジット1人分 = クレジット(M) / 育成基準
      - クレジット基準値 = (レポート人数 / 2) × 育成基準
      - レポート基準人数人分以上かつクレジットがクレジット基準値より少ない場合に「クレジット推奨(約クレジット基準値Mまで)」
      - それ以外は「レポート推奨」
      - 注意書き: 「※基本的にはレポート周回した方が効率が良いとされています」(クレジット推奨時のみ表示)
    - 「レポート」グループ内に以下を表示:
      - 合計EXP / 1249200 を「レベル90可能な人数」として「約 # 人分」形式で表示（切り捨てで小数点第1位まで）。
      - 初級/中級/上級/最上級の各レポートを「約 # 人分」形式で表示。
        - 初級 = 初級レポート / 25000
        - 中級 = 中級レポート / 2500
        - 上級 = 上級レポート / 625
        - 最上級 = 最上級レポート / 125
        - 有効桁数は切り捨てで小数点第1位まで。
      - 合計EXP（3桁ごとにカンマ区切り）。
      - EXP×7 を「消費に必要なクレジット」として「約 #」形式で表示し、入力クレジットより多い場合は赤色で表示。
    - 「クレジット」グループ内に以下を表示（「約 # 人分」形式、切り捨てで小数点第1位まで）:
      - クレジット数 / 95400716 → 完全育成可能な人数
      - 基準まで育成可能な人数: 約 クレジット(M) / 育成基準 人
      - クレジット数 / 8744295 → レベル90可能な人数
      - クレジット数 / 40017500 → スキル最大可能な人数
      - クレジット数 / 5090088 → 装備最大可能な人数
      - クレジット数 / 14628900 → 固有4まで育成可能な人数
      - クレジット数 / 10500000 → WB25まで育成可能な人数
    - 入力が無効な場合はエラーメッセージを表示。
  - **動作**:
    - 入力停止後0.5秒のデバウンスで自動計算。
    - 計算ボタンでも即時計算可能。
  - **ローカルストレージ**:
    - 保存する値: 初級/中級/上級/最上級レポート数、クレジット、基準人数、育成基準。
- **レイアウト**:
  - シンプルな中央配置フォーム。
  - レスポンシブデザイン (モバイル対応)。
- **スタイル**:
  - タイトルバー、ハンバーガーメニュー、メニューのスタイルを定義。
  - タブ風ラベルのホバー/選択時の配色とボーダー挙動を記述。
  - ダークモード対応（CSS変数で配色切替）。
  - 結果の強調色（リンクカラー）と数値のカンマ区切り表示。

## 実装計画
### プロジェクト構造
```
ts/
├── index.html                     # メインHTMLファイル
├── package.json                   # 依存関係
├── tsconfig.json                  # TypeScript設定
├── tsconfig.node.json             # Node向け設定
├── vite.config.ts                 # Vite設定
├── vitest.config.ts               # Vitest設定
├── public/                        # 静的アセット
├── src/
│   ├── main.ts                    # エントリーポイント
│   ├── app.ts                     # メインアプリ (アプリスイッチャー)
│   ├── sub-app.ts                 # サブアプリ共通インターフェース
│   ├── helper-calc-app.ts         # 助っ人貸出回数計算機 UI
│   ├── calculator.ts              # 助っ人貸出回数の計算ロジック
│   ├── tactical-rank-calc-app.ts  # 対抗戦順位計算機 UI
│   ├── tactical-rank-calc-calculator.ts # 対抗戦の計算ロジック
│   ├── report-calc-app.ts         # レポート計算機 UI
│   ├── storage-manager.ts         # ローカルストレージ一元管理
│   └── style.css                  # スタイル
└── test/
  ├── calculator.test.ts                 # 助っ人計算ロジックのテスト
  └── tactical-rank-calc-calculator.test.ts # 対抗戦計算ロジックのテスト
```

### 実装ステップ (ライブコーディング順)
1. **プロジェクト初期化**: pnpm + Vite + TypeScript のセットアップ。
2. **サブアプリ共通IF**: `sub-app.ts` にインターフェースを定義。
3. **メインアプリ**: `app.ts` でアプリスイッチャーを実装（タイトル更新・メニュー制御）。
4. **サブアプリ**: `helper-calc-app.ts` と `tactical-rank-calc-app.ts` を実装（UI + 入力処理）。
5. **計算ロジック**: `calculator.ts` と `tactical-rank-calc-calculator.ts` を実装。
6. **ストレージ**: `storage-manager.ts` でローカルストレージの一元管理を実装。
7. **HTML構造**: `index.html` を作成。
8. **エントリーポイント**: `main.ts` でアプリ起動。
9. **スタイリング**: `style.css` でUI装飾（ダークモード対応）。
10. **テスト**: Vitestでユニットテストを追加・実行。

### 考慮点
- **エラーハンドリング**:
  - 入力値のバリデーション（数値チェック、範囲チェック）。
  - カスタムエラークラス: `InvalidInputError`, `NegativeLoanError`, `OutOfRangeError`（助っ人計算ロジック）。
  - UI層で追加検証: 無効な日時、未来日時はメッセージ表示（助っ人アプリ）。
- **パフォーマンス**: 計算は軽量なので最適化不要。
- **拡張性**: 将来的に追加機能 (例: 詳細なログ) を考慮したモジュール化。
- **アクセシビリティ**: ラベル付け、キーボード操作対応。
- **ストレージ管理**: `storage-manager.ts` でキーと操作を一元管理し、各サブアプリから利用。
- **用語統一**: 「貸出回数」で統一（UI表示は「編成回数（推定）」）。

## テストケース

### サブアプリ: 助っ人貸出回数計算機 (HelperCalcApp)
- 入力: 登録時間=161, 獲得報酬=1,544,260 → 登録時間報酬=194,260, 貸出回数=27, 貸出回数報酬=1,350,000 (PASS)
- 入力: 登録時間=70, 獲得報酬=784,480 → 登録時間報酬=84,480, 貸出回数=14, 貸出回数報酬=700,000 (PASS)
- 入力: 登録時間=176, 獲得報酬=6,711,560 → 登録時間報酬=211,560, 貸出回数=130, 貸出回数報酬=6,500,000 (PASS)
- 入力: 登録時間=1, 獲得報酬=100,000 → 範囲外 → OutOfRangeError「獲得報酬の値が計算範囲外です」 (PASS)
- 入力: 登録時間=0, 獲得報酬=0 → 登録時間報酬=0, 貸出回数=0, 貸出回数報酬=0 (PASS)
 - 入力: 無効な日時 / 未来日時 → UIで「無効な日時です」「未来の日時は無効です」を表示（結果非表示）

### サブアプリ: 対抗戦順位計算機 (TacticalRankCalcApp)
- calculateMultipleRanks:
  - 入力: 15001, 25 → 出力: [10500, 7350, 5145, 3601, 2520, 1764, 1234, 863, 604, 422, 295, 206, 144, 100, 70, 49, 34, 23, 16, 11, 7, 4, 1]
 - 入力変更時の自動計算とボタンクリック計算の両対応を確認

### サブアプリ: レポート計算機 (ReportCalcApp)
- 入力: 初級=1, 中級=2, 上級=3, 最上級=4 → EXP=1*50+2*500+3*2000+4*10000=50, 1000, 6000, 40000 合計47050
- 入力: 空欄は0扱い、整数以外/負数はエラー表示
- 入力変更時の自動計算とボタンクリック計算の両対応を確認
